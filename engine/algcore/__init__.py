"""Vendored verbatim copies of the reference algorithm implementations from
`<repo>/algorithms/`.  The dashboard engine layers (planner/coordinator/avoidance/
allocator/comms) call into these so the live console runs the real algorithms,
not the earlier cut-down approximations.

Do not edit these files here; edit `algorithms/` and re-copy.
"""
